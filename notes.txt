
ssh-keygen -t rsa-b 4096 -C "hari646592@gmail.com"
eval "s(ssh-agent -s)"
ssh-add-/.ssh/id rsa
cat /.ssh/id_rsa.pub (then copy the content)


git config --global user.name "hari.krishna"
git config --global user.email "hari646592@gmail.com"

git config --list

ssh-keygen -t ed25519 -C "hari646592@gmail.com"


eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
cat ~/.ssh/id_ed25519.pub
Copy the output, then:

Go to GitHub → Settings → SSH and GPG keys → New SSH key


Test connection:

ssh -T git@github.com


If successful, you’ll see:

Hi <username>! You've successfully authenticated...